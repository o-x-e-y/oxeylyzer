# Corpora

Corpora go here. To add one, add a folder with the name of the corpus you want. Add any number of text files inside that folder and run `load <folder name>` in the repl. For more info, see the main README document.
